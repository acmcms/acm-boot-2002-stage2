# acm-boot-2002-stage2

Legacy bootstrap code from the AE1 generation (`src/ae1core` — `ServerManager`/`ServerInitializer`/`InterfaceStarter`, plus `*.xml.sample` config templates), older than `acm-boot-2002-stage1` (AE2). Historical/legacy — verify anything still actually depends on this before assuming it's live.
